# --- PostgreSQL ---
POSTGRES_USER=flightpulse
POSTGRES_PASSWORD=flightpulse
POSTGRES_DB=flightpulse
POSTGRES_PORT=5432
DATABASE_URL=postgresql://flightpulse:flightpulse@localhost:5432/flightpulse

# --- Redis ---
REDIS_PORT=6379
REDIS_URL=redis://localhost:6379/0

# --- OpenSky Network API ---
# The official docs recommend OAuth2 client-credentials for programmatic access.
# https://openskynetwork.github.io/opensky-api/rest.html
OPENSKY_CLIENT_ID=
OPENSKY_CLIENT_SECRET=
OPENSKY_TOKEN_URL=https://auth.opensky-network.org/auth/realms/opensky-network/protocol/openid-connect/token
OPENSKY_API_BASE_URL=https://opensky-network.org/api

# --- FastAPI ingestion service ---
INGESTION_HOST=0.0.0.0
INGESTION_PORT=8001

# --- Collector (Phase 2) ---
# Conservative polling per OpenSky's documented rate limits.
POLL_INTERVAL_SECONDS=30
COLLECTOR_ID=collector-1
# Where the collector sends normalized batches. Points at the ingestion
# service directly until the load balancer (Phase 3) exists, then should
# point at the load balancer instead.
TELEMETRY_TARGET_URL=http://localhost:8001/telemetry

# --- Load balancer (added in a later phase) ---
LOAD_BALANCER_PORT=8080
